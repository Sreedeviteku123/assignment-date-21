#include<stdio.h>
int main()
{
	int n,num,rev=0;
	scanf("%d",&n);
	num=n;
	while(n!=0)
	{
		rev=rev*10+n%10;
		n=n/10;
	}
	if(rev==num){
	printf("%d is pilendrome",num);
}
	else
	{
	printf("%d is not pilendrome",num);
}
}
